# Weather-Prediction
A full-stack weather app built with a Python (Flask) backend and React + TypeScript frontend. Fetches live weather and 5-day forecasts via the free Open-Meteo API, with city search, favorites, and unit conversion.
